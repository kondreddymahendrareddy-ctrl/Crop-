# Environment variables for Intelligent Crop System

# Weather API Key (Optional for Open-Meteo as it is free without key, 
# but useful if you switch to OpenWeatherMap)
WEATHER_API_KEY=your_api_key_here

# Database URI (if switching from sqlite)
DATABASE_URL=sqlite:///database/crop_system.db
